# Narrow CRS tuning: for /auth/login_flow, skip rule 920420 (content-type allowlist)
modsecurity_rules '
  SecRule REQUEST_URI "@streq /auth/login_flow" \
    "id:1000421,phase:1,pass,nolog,ctl:ruleRemoveById=920420"
';
